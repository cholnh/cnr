spring:
  application:
    name: copsNrobbers
  config:
    import:
      - application-rds.yml
      - application-redis.yml
  profiles:
    active: local

server:
  tomcat:
    uri-encoding: UTF-8
    threads:
      max: 500