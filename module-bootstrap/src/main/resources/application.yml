spring:
  application:
    name: copsNrobbers
  config:
    import:
      - application-rds.yml
      - application-cache.yml
  profiles:
    active: local

server:
  tomcat:
    uri-encoding: UTF-8
    threads:
      max: 500
  port: 8081